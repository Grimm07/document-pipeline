# Cocogitto — conventional commits validation
# https://docs.cocogitto.io/
#
# Used by lefthook commit-msg hook: cog verify --file <path>
# Changelog is handled by git-cliff (cliff.toml), not cocogitto.

from_latest_tag = false
tag_prefix = "v"
disable_changelog = true
disable_bump_commit = true
ignore_merge_commits = true
